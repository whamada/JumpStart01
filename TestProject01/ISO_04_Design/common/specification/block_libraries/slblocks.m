function blkStruct = slblocks

%   Copyright 2021 The MathWorks, Inc.

% NOTE: To enable a user library to appear in the Library Browser, you must
% first set its "EnableLBRepository" property to "on" using the following
% command:
%   set_param(lib, 'EnableLBRepository', 'on')
% See the instructions by running the following command:
%   web(fullfile(docroot, 'simulink/ug/adding-libraries-to-the-library-browser.html?s_tid=doc_srchtitle'))

% Browser(1).Library = 'userLib1';
% Browser(1).Name = 'User Library 1';
% Browser(2).Library = 'userLib2';
% Browser(2).Name = 'User Library 2';
% blkStruct.Browser = Browser;

blkStruct.Browser = []; % To be removed by user.

end
